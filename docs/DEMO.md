# Пятиминутное демо HackAlem AI

## Кандидат и проверенный состав

Сценарий относится к React-выпуску с кодовым SHA `a086162e909c080890a2a16807723a3c00a60ab2`. Фактический артефакт — `out/release-a086-1/` из worktree `wt-react-integration`; его открывали в Chrome через localhost. Команды ниже создают отдельный свежий выпуск `out/demo-a086162/`, поэтому проверенный артефакт `release-a086-1` остаётся нетронутым.

В кандидат входят React/Vite/vis-network, Python batch, HA-17 (причины ролей и приоритета, secondary-role фильтр, точные ties) и HA-18 (осторожная описательная гипотеза кластера). HA-11, HA-14, HA-16 и HA-13 в этом выпуске **отсутствуют**; в показе не обещать дневные профили, запросы по frontier, временные маршруты или межкластерные потоки/SCC.

На официальной выборке: 1–31 июля 2026 года, операции от 5 000 KZT и до четырёх шагов от известных seed. В финальном выпуске 2 248 узлов, 3 119 направленных рёбер, 4 840 транзакций, 81 seed, 89 кластеров и 19 изолятов. Это структура переданной выборки, не полный граф банка.

## Подготовка до аудитории

Команды выполняются из корня репозитория. Образ собрать заранее, так как загрузка базовых образов и зависимостей требует сети. После успешной сборки выполнить обязательный Docker cleanup; он сохраняет контейнеры и volumes.

```powershell
$env:HACKALEM_IMAGE = 'jigas-final-a086162:local'
$env:HACKALEM_CPUS = '12'
docker compose build pipeline
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "G:\Tools\DockerCleanup\Docker-Cleanup.ps1" -Automatic
docker compose up -d viewer
```

Заранее убедиться, что официальный архив `track_data/data (1).zip` на месте, порт `127.0.0.1:8000` свободен, а viewer запущен. Не открывать страницу через `file://`: React загружает соседний `report.json` с same-origin localhost.

## Команды свежего выпуска

В начале показа запустить подготовку входных Parquet, затем batch на официальном архиве. Сборка образа остаётся за пределами таймера выступления.

```powershell
docker compose run --rm prepare

$timer = [System.Diagnostics.Stopwatch]::StartNew()
docker compose run --rm pipeline --data /app/data --out /app/out/demo-a086162
$pipelineExit = $LASTEXITCODE
$timer.Stop()
"pipeline exit={0}; elapsed={1:N3} s" -f $pipelineExit, $timer.Elapsed.TotalSeconds
```

`prepare` создаёт `nodes.parquet`, `edges.parquet` и `transactions.parquet`. Успешный pipeline создаёт три CSV, `report.json`, `report.html`, `validation.json` и локальные JS/CSS/favicon/license в `assets/`. После него открыть `http://127.0.0.1:8000/demo-a086162/report.html`.

Для проверки перед выступлением сначала создать свежий `demo-a086162` тем же pipeline, затем выполнить контрактную проверку. Она не входит в пятиминутный показ:

```powershell
docker compose run --rm --entrypoint python pipeline test_contract.py --data /app/data --out /app/out/demo-a086162
```

Пять замеров готового Docker image из [протокола проверки](validation.md): 4,508 / 4,619 / 5,656 / 5,621 / 4,454 с; медиана 4,619 с, максимум 5,656 с при лимите 12 CPU. Они включают запуск контейнера с pipeline, но исключают `prepare`, сборку образа, работу браузера и речь. Это batch timing, не длительность демо.

## Рассказ и действия на пять минут

Отдельный таймер всего выступления (например, на телефоне) запускается непосредственно перед `prepare` и останавливается после заключительной реплики. Сборка образа и запуск viewer сделаны заранее; вычисление нового отчёта входит в эти пять минут. PowerShell-переменная `$timer` в блоке команд выше измеряет только pipeline после `prepare`.

| Время | Действие и короткий текст для рассказа |
|---|---|
| 0:00–0:35 | Показать официальный архив и выполнить `docker compose run --rm prepare`. «Сначала читаем исходную выгрузку за июль 2026 года. Это не полная база банка: в расчёт попали операции от 5 000 тенге, не дальше четырёх шагов от 81 известных seed. Значит, дальше мы обсуждаем только связи, которые действительно присутствуют в этом срезе. Любой вывод за его границей потребует дополнительных данных». |
| 0:35–1:00 | Выполнить приведённую выше команду `pipeline`, показать `exit=0`, `validation.json: success` и сводку: 2 248 узлов, 3 119 рёбер, 4 840 операций, 89 кластеров. «Здесь не заранее сохранённый макет: контейнер прочитал Parquet и сформировал новый выпуск. Секундомер рядом измеряет только batch. Оценки и сортировку рассчитывает Python, интерфейс их не меняет». |
| 1:00–1:30 | Открыть `127.0.0.1:8000/demo-a086162/report.html`, показать период и сводку на настоящем направленном canvas. «Страница читает `report.json` и локальные assets из того же выпуска через localhost. Для показа не нужны внешний API, удалённые скрипты или подключение к интернету. Цвет и форма помогают ориентироваться, стрелка задаёт направление ребра». |
| 1:30–2:20 | Ввести полный ID `100000003684369100`. В карточке показать координатора, `role_score=0.600`, `priority_score=0.965`, 24 входящие и 62 исходящие связи; среди совпавших вторичных правил — distributor и consolidator. «Этот клиент стоит первым в списке приоритета. Объяснение раскрывает признаки правила и вклады, по которым модель поставила его выше. `role_score` показывает поддержку правила, а `priority_score` — порядок следующей проверки. Ни одна из этих оценок не является вероятностью нарушения или выводом о виновности». |
| 2:20–3:25 | Показать направление связей и выбор узла в canvas/таблице; при удобном масштабе переключить вид на полный граф 2 248/3 119. «Поиск сравнивает полный ID как строку: длинный номер не округляется. Выбор синхронно открывает карточку и выделяет узел; в соседней таблице можно сверить конкретные исходящие и входящие рёбра, сумму и глубину. Полный граф помогает увидеть структуру, а локальный режим — рассмотреть связи выбранного узла. Наблюдаемая сумма относится только к операциям из переданного файла». |
| 3:25–4:15 | Прокрутить к кластеру 2 и раскрыть его. Показать 138 узлов, 3 seed, 152 внутренних ребра и внутренний наблюдаемый оборот 18 838 290.00 KZT. «HA-18 добавляет к сводке аккуратное описание лидера и группы: это первый кандидат для проверки по P и наблюдаемые входящие/исходящие связи. Это способ понять, с чего начать проверку, а не утверждение, что группа организована для нарушения. HA-17 показывает основания ролей и приоритета, а также равные значения P; эти объяснения не меняют численное ранжирование». |
| 4:15–4:45 | Показать ссылки `nodes_roles.csv`, `clusters.csv`, `top_nodes.csv`; рядом доступны `report.json`, `report.html` и `validation.json`. «Три CSV создаются из того же отчёта и позволяют выгрузить роли, сводки кластеров и top приоритетов. JSON — источник тех же данных для интерфейса. В манифесте также лежат проверка выпуска и локальные ресурсы страницы». |
| 4:45–5:00 | Завершить границей данных и способом сдачи. «Например, depth=4 узел `100000000018102100` не имеет исходящих связей в этой выборке. Это не доказывает конечное назначение денег: дальше выгрузка не показывает. HA-11/14/16/13 в релиз не входят. Проект сдаём только через командный GitHub; веб-формы нет». |

## Репетиция ответа на три произвольных GID

Отдельно от основного сценария потренируйте вопрос жюри: за минуту объяснить роль трёх названных узлов по метрикам. Это примеры для репетиции из принятого `a086162` (`out/release-a086-1/report.json`); на защите жюри может выбрать другие ID.

- `100000000018102100` — `peripheral`, `role_score=0`: depth=4, одно входящее ребро на 7 000 KZT, исходящих рёбер и суммы нет. В этом срезе виден узел на границе наблюдения.
- `100000000031787100` — `transit`, `role_score=0.65`: depth=2, входящая и исходящая суммы по 90 000 KZT, `r=1`. Равный наблюдаемый поток согласуется с транзитной ролью.
- `100000000041368100` — `terminal`, `role_score=0.5`: depth=1, входящая сумма 160 000 KZT, `out_deg=0`, прошло 9 дней после последнего входа. В наблюдаемом периоде исходящих рёбер нет.

Это учебные примеры, а не подсказка о выборе жюри; роли описывают только наблюдения в архиве. Тренируйте этот разбор отдельно; если жюри задаст такой вопрос во время защиты, уложите ответ в общий лимит 5 минут, сократив подготовленные пояснения.

## Проверенные факты и честные ограничения

- ID `100000003684369100` — первая строка `top_nodes.csv`: coordinator, роль 0.600, приоритет 0.965, кластер 2; число также найдено поиском в `report.json` и показано в React-карточке.
- Кластер 2: 138 узлов, 3 seed, 152 внутренних ребра, `sum_kzt_internal=18838290.00`. Гипотеза HA-18 опирается на проверяемые поля лидера и группы и формулируется как описание наблюдений.
- Граничный пример `100000000018102100`: depth=4, одно входящее ребро на 7 000 KZT, ноль наблюдаемых исходящих. Не экстраполировать за пределы архива.
- Лимиты входа: только выбранный период, порог суммы и четыре шага; неполный вход seed и нулевой исходящий поток на frontier не устанавливают конечное назначение денег.
- Для продукта не заявлены AML accuracy/precision/recall и масштабирование до миллиона узлов: размеченного ground truth для этих выводов нет.
- Включены HA-17 и HA-18. HA-11/14/16/13 отсутствуют целиком; не называть их будущие блоки «нулевыми» и не показывать исследовательские числа как результат выпуска.
- Публикация/подача проекта подтверждается только фактом GitHub push через командную строку; эта инструкция не утверждает, что подача уже выполнена.

## Фактический browser walkthrough

23.09.2026 Chrome 153 / Playwright открыл read-only `out/release-a086-1/report.html` из `wt-react-integration` по `http://127.0.0.1:8000/release-a086-1/report.html`. Проверены HTTP 200, настоящий canvas 1 078×490, точный поиск `100000003684369100`, полный граф 2 248/3 119, раскрытие кластера 2 с HA-18-гипотезой и все три CSV (HTTP 200, ненулевые ответы). Ошибок страницы/консоли и внешних запросов — 0. Автоматизированная последовательность от открытия страницы до трёх CSV заняла **5,139 с**; это только browser walkthrough, без batch-команд и речи.

Этот результат не является устной пятиминутной репетицией. Речь и паузы реального демонстратора агент не произносил; капитану нужно один раз пройти таблицу с физическим таймером и записать фактическое время. Для свежего `demo-a086162` отдельный batch секундомер показывает только pipeline; время `prepare` и полный устный проход фиксируются отдельно.

## Подача проекта

Пользователь подтвердил: подавать проект только через командный GitHub, без формы. После review/интеграции финального коммита капитан проверяет рабочее дерево и публикует согласованный `main` через Git:

```powershell
git status --short --branch
git rev-parse HEAD
git push origin main
```

В Trello/отчёте отмечать подачу только после успешного push и подтверждённого commit SHA. Эта карточка готовит сценарий демо и сама по себе не подтверждает факт подачи.
