# Параллельная разработка HackAlem AI

Рабочая координация: [доска Trello](https://trello.com/b/6tCEnwZQ/hackathon), затем Git-ветки и handoff в карточке. Текущий код и спецификации всегда сверяются с `origin/main`; карточки могут обновляться параллельно. Одновременное создание HA-15 было согласовано: миграция React — HA-15, объяснения — HA-17, маршруты — HA-16. URL карточек сохранены; при передаче используйте полную ссылку.

На 23.09 исследовательский контракт PRODUCT_SPEC 1.3 / SPEC 1.4 / PLAN 1.3 включён в `origin/main` (`2e53d60`). После `git fetch origin` проверьте текущий HEAD и сверяйте карточку с опубликованным контрактом.

**Текущий срез, 16:34 Астана:** `main@1d27281` содержит React-граф, выпуск JSON/CSV, HA-17 и HA-18; пять официальных Docker запусков, контрактные проверки и локальный Playwright прошли. HA-15.1/15.2/15.3, HA-17.1/17.2, HA-18 и HA-12.2 имеют независимый review и стоят в Trello «Готово». Очередь «На проверке» пуста на момент среза. P0-дефект строкового JSON `role_score` закрыт в `main@1dd1c06`; HA-08.1 ещё дополняет единый контракт проверками HA-18, HA-08.2 проверяет offline/чистую сборку, HA-09.2 проверяет комплект сдачи. Ниже сохранён срез 16:10 как история зависимостей; его формулировки о незавершённом React уже устарели. Перед работой перечитайте живую доску.

## Срез блокеров · 23.09.2026, 16:10 Астана

Источник статуса на момент первого среза — [доска Trello](https://trello.com/b/6tCEnwZQ/hackathon) после `git fetch origin` (`origin/main` тогда был `59ef08e`). Этот срез помогает новым чатам начать с верной зависимости; перед работой перечитайте карточку, так как статусы меняются.

| Задача | Что мешает закрыть | Следующий проверяемый шаг |
|---|---|---|
| [HA-15.1](https://trello.com/c/Rnk792mE) | Независимый [review](reviews/HA-15.1-2026-09-23.md) `23faa02` прошёл: сборка, точные ID, JSON-ошибки, клавиатура и CSV-ссылки проверены. Граф ещё заглушка HA-15.2. | Передать App-owned `selectedGid`/filters HA-15.2 и проверить реальный граф отдельно. |
| [HA-17.1](https://trello.com/c/tIzQHdJ0) | Независимый [review](reviews/HA-17.1-2026-09-23.md) `7a6da9b` прошёл: все reason/evidence/why и численная неизменность на официальном payload подтверждены. | Передать JSON shape HA-17.2; выполнить отдельную проверку UI и итогового выпуска. |
| [HA-15.2](https://trello.com/c/kpeqbuVO), [HA-15.3](https://trello.com/c/TOLPtvoW) | React-граф и выпуск `report.json`/статики ещё «В работе»; общего финального SHA нет. | Владельцам собрать один реальный raw → localhost → поиск/граф → CSV выпуск, передать commit и результаты C. |
| [HA-17.2](https://trello.com/c/Cv7bLi2c) | Нужны принятая HA-17.1 и реальный выпуск HA-15.2/15.3. | После handoff показать причины, суммы рёбер и равные приоритеты на реальном JSON; проверить в браузере. |
| [HA-08.1](https://trello.com/c/VehOaQmX) | На `feat/verification@c67aebe` исправлена проверка `validation.json status=success` и добавлены проверки assets. [P0 дефект](https://trello.com/c/3mU1Y33G): тест принимает строковый `nodes[].role_score` в `report.json`; изолированный fix опубликован в PR #5. | Verification-владельцу принять fix, затем повторить отрицательную пробу и полный PASS на интегрированном SHA. |
| [HA-08.2](https://trello.com/c/9OP9LTPB) | Пять прежних PASS относятся к standalone P0 (`aed8e63`), поэтому не подтверждают React-кандидат. | После заморозки состава провести пять полных запусков, контракт и offline localhost browser на зафиксированном SHA; записать median/max в `docs/validation.md`. |
| [HA-09.1](https://trello.com/c/KCsKqr27) | На исходной `feat/verification` README описывал запуск, но не содержал таблицы критериев ролей/порогов и масштабирования до ~1 млн узлов из первичного ТЗ. Изолированный fix опубликован в PR #6. | Verification-владельцу принять текст, затем сверить итоговые команды/localhost со сборкой HA-15.3. Не менять согласованные формулы ради текста. |
| [HA-09.2](https://trello.com/c/OCpuEnws) | Финальный манифест и лицензии зависят от фактически принятого SHA и результатов HA-08.2. | Сверить README, архитектуру, assets/licenses и состав сдачи после финальной проверки. |
| [HA-11.1](https://trello.com/c/etDrIuJu), [HA-11.2](https://trello.com/c/FpIk7AHb) | Аналитика HA-11.1 прошла review в отдельной ветке, но не вошла в общий кандидат; старый HTML для HA-11.2 не принят. | Не задерживать P0: P1-пару включать только целиком в React, если она успевает пройти проверку до freeze. |

До **16:30 Астана** завершайте только уже начатое P0 и его review. После freeze — интеграция, исправления P0, испытания и демо. Незавершённые P1 остаются вне релиза. Не переносите карточку в «Готово» по одному опубликованному коммиту или этому срезу.

**Обновление ~16:20.** HA-15.1 и HA-17.1 прошли независимый review и отмечены «Готово» в Trello; их код ещё должен попасть в общий выпуск. Для дефекта HA-08.1 готов изолированный [PR #5](https://github.com/BAITC-Hacks/hack-e3bf6094-jigas/pull/5) к `feat/verification`: строгие JSON score types и отрицательная fixture-проба прошли, финальный React batch ещё не проверен. Для README HA-09.1 готов [PR #6](https://github.com/BAITC-Hacks/hack-e3bf6094-jigas/pull/6) к той же ветке: критерии ролей/пороги и честный план масштабирования. Verification-владелец принимает эти патчи и проверяет итоговую ветку; draft PR не означают включение в релиз.

## Как занять задачу

1. Откройте `HA-CTX`, актуальные `docs/SPEC.md`, `docs/PRODUCT_SPEC.md`, `docs/PLAN.md` и список задач. Проверьте даты обновления и зависимости. Если Git и Trello расходятся, уточните контракт в карточке и не меняйте формулы наугад.
2. Выберите **одну** доступную, не занятую другим чатом карточку с отдельным результатом и файлами либо независимую проверку карточки из «На проверке». Проверьте стартовые зависимости; блокированную функцию не начинайте. До кода запишите в её описание: контур/чат, ветку, base SHA, файлы, критерий проверки и следующего получателя. Рабочую задачу переместите в «В работе». Участника Trello не назначайте.
3. Создайте отдельный `git worktree` и уникальную ветку от свежего `origin/main`. Не меняйте `main` напрямую. Если файл уже захвачен другой карточкой, выберите независимую задачу или дождитесь handoff.
4. Через 20–30 минут обновите карточку кратким промежуточным результатом или конкретным блокером. После реализации запушьте **только свою ветку**, запишите commit URL, команды и фактические результаты тестов, риски и следующий шаг. Результат переместите в «На проверке»; «Готово» — после независимого review.
5. Интегратор этого потока принимает ветки в `main` после review и общего Docker/browser прогона. Перед новой работой делайте `git fetch origin` и перечитывайте карточку: другой чат мог обновить контракт.

## Владение файлами на текущем этапе

| Область | Владелец сейчас | Другим чатам |
|---|---|---|
| `frontend/`, `hackalem/pipeline.py`, `hackalem/html.py`, `hackalem/scoring.py`, `hackalem/report.py`, Dockerfile, compose | Интегрированы в `main` после review и Docker/browser проверки | Новые функции после freeze не начинать; дефекты брать через отдельную карточку |
| `test_contract.py` и HA-08.1 | Агент `/root/ha08_contract` дополняет проверку HA-18 | Не править параллельно до handoff |
| `README.md`, `docs/validation.md` | Интегратор этого потока | Документальные исправления передавать через отдельный handoff |
| `docs/SECOND_WAVE_RESEARCH.md`, `research/second_wave*`, обновляемые SPEC/PLAN | Параллельный исследовательский поток | Не править до handoff |

React-выпуск включён в `main` и прошёл пять Docker прогонов, контрактный тест и локальный Playwright smoke; текущий SHA всегда проверяйте через `git fetch origin`. Заморозка новых функций наступила **16:30 по Астане**: до сдачи в 18:00 берите только проверки, исправления дефектов, документацию и демо. Живые статусы — в Trello; раздел выше фиксирует текущие владения файлами.

## Docker в нескольких worktree

`compose.yaml` принимает `HACKALEM_IMAGE` и `HACKALEM_CPUS`. По умолчанию образ `jigas-graph-money:local`, лимит pipeline — 12 CPU. Для одновременной работы задавайте **уникальный образ на worktree** и делите общие 12 логических CPU между запущенными pipeline. Пример PowerShell:

```powershell
$env:HACKALEM_IMAGE = 'jigas-ha10-chat2:local'
$env:HACKALEM_CPUS = '4'
docker compose build pipeline
docker compose run --rm prepare
docker compose run --rm pipeline
```

После **каждой успешной локальной Docker-сборки** выполните `powershell.exe -NoProfile -ExecutionPolicy Bypass -File "G:\Tools\DockerCleanup\Docker-Cleanup.ps1" -Automatic`. Контейнеры и volumes автоматически не чистите. Исходный архив/данные, секреты и generated `data/`/`out/` не добавляйте в коммит сверх уже отслеживаемых файлов.

## Промпт для отдельного чата

```text
Работаем в G:\HACKATON\hack-e3bf6094-jigas. Цель — подготовить HackAlem AI к сдаче 23.09.2026 18:00 по Астане. Сначала git fetch origin, затем прочитай актуальные docs/PARALLEL_WORK.md, SPEC.md, PRODUCT_SPEC.md, PLAN.md и Trello https://trello.com/b/6tCEnwZQ/hackathon. Проверь текущий origin/main: React-выпуск и HA-17/HA-18 уже интегрированы, новые функции после freeze 16:30 не начинай. Задачи и исследования обновляются параллельно; идентифицируй задачу по полной URL.

Выбери одну доступную незанятую карточку по проверке, исправлению дефекта, документации или демо либо независимый review карточки из «На проверке». Перечитай живой статус доски перед захватом: очередь меняется. Проверь зависимости и владение файлами в docs/PARALLEL_WORK.md и Trello; test_contract.py сейчас занят HA-08.1. Запиши в карточку ветку, base SHA, файлы и критерий; рабочую задачу перемести «В работе», но никого из участников не назначай.

Создай отдельный git worktree и уникальную feature-ветку от origin/main. Если работа допускает параллельные независимые части, заспавни субагентов gpt-6-luna с reasoning_effort=max, каждому отдельный worktree и непересекающиеся файлы. Не пушь main: после проверки пушь свою ветку и оставь Trello handoff с commit URL, командами/результатами тестов, рисками и следующим получателем; готовность — «На проверке» до независимого review. Каждые 20–30 минут обновляй статус. Если новые SPEC/Trello заменили задачу, останови устаревшую реализацию и передай состояние.

Для Docker используй уникальный HACKALEM_IMAGE на worktree, HACKALEM_CPUS так, чтобы одновременные запуски вместе укладывались в 12 CPU. После каждой успешной сборки запускай обязательный Docker-Cleanup.ps1 -Automatic, не трогай volumes. В конце дай мне ссылку на ветку и карточку для интеграции в основной поток.
```
