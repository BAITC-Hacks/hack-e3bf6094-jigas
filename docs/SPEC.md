# Техническая спецификация MVP

Версия 1.4 · 23.09.2026 · **контракт для реализации, не описание готового кода**. Приняты миграция React/Vite (HA-15), объединение blueprint-ов и приоритетные изменения [второй волны](SECOND_WAVE_RESEARCH.md): объяснения/читаемость (HA-17, P0), ранний сценарий запросов и маршруты с датами (HA-16, P1). Базовые роли, формула P, CSV и `schema_version=1.0` сохранены; JSON получает совместимые дополнительные поля.

[Продукт](PRODUCT_SPEC.md) · [План и задачи](PLAN.md) · [Исследование Astra](../HackAlem_AI_Research_Architecture_Blueprint.md) · [Сравнение и доказательства объединения](BLUEPRINT_COMPARISON.md). Решения этого контракта имеют приоритет над альтернативами в исследовательских blueprint-ах.

**Дополнение release-review, 23.09.2026:** приняты §6.1 (HA-18, небольшой P1) и уточнения приёмки §9; [решение и расчёты](RELEASE_REVIEW_RESPONSE_2026-09-23.md). Версия базового контракта 1.4 и schema_version 1.0 сохраняются; роли, support, P и порядок равных P не меняются.

## 1. Исполнение и файлы

Основной контейнерный запуск на машине команды: `docker compose build pipeline`, `docker compose run --rm prepare`, `docker compose run --rm pipeline`. Сервис `prepare` извлекает три Parquet из архива организатора, без Python на хосте. Равнозначный запуск для эксперта без Docker после установки зависимостей: `python scripts/prepare_data.py`, затем `python starter.py --data ./data --out ./out`. Оба пути читают те же три Parquet и пишут в `out/`; Docker не должен быть единственным способом проверки.

Выбранный стек: Docker Compose и Python 3.13, pandas, NumPy, PyArrow, NetworkX; React + Vite (JSX и обычный CSS), Cytoscape.js для графа. Python-зависимости зафиксированы в `requirements.txt`. Версии frontend-зависимостей и совместимый Node зафиксированы в `frontend/package.json`, lockfile и инструкции сборки. Для одного экрана достаточно React state; router, глобальный store, UI-kit и сервер приложения в объём не входят. Исходный HTML-renderer оставлен в истории проекта; текущий выпуск включает React UI.

Порядок: загрузка → валидация → полный граф → признаки → роли/приоритет → сообщества/сводки → проверка результата → выпуск CSV/JSON и копирование заранее собранного UI. Кластеры могут вычисляться независимо от ролей, `top_gids` формируется после приоритета.

Реализованные P1-признаки и структурный контекст добавляются после назначения кластеров, до сборки/проверки общего отчёта. Их читает тот же React-интерфейс. У них нет собственного расчёта ролей, приоритета или второго выпуска CSV; вычисление остаётся конечным batch CLI, просмотр — отдельной раздачей статических файлов без API.

Результат успешного запуска: `nodes_roles.csv`, `clusters.csv`, `top_nodes.csv`, строгий UTF-8 `report.json`, одинаковые `index.html` и `report.html`, каталог `assets/` собранного UI и небольшой `validation.json`. Код возврата 0 означает, что обязательные файлы сформированы и проверены. Ошибка входа/расчёта/записи или отсутствующий frontend build даёт ненулевой код и этап ошибки. Сначала писать и проверять временный выпуск внутри целевого out-mount, включая ресурсы UI; `validation.json` со статусом успеха публикуется последним. Не выдавать смешанный набор старых и новых файлов за успешный результат.

`starter.py` остаётся совместимым фасадом `hackalem/`, `python -m hackalem` — равнозначным CLI.

Сборка UI выполняется один раз на версию кода, не на каждый датасет. Целевые команды после HA-15: `npm --prefix frontend ci`, `npm --prefix frontend run build`, существующий Python CLI, затем `python -m http.server 8000 --bind 127.0.0.1 --directory ./out`. Адрес — `http://127.0.0.1:8000/` (или `/report.html`). Docker собирает UI на отдельной Node-стадии и копирует результат в Python-образ; сервис `viewer` раздаёт только `out/` read-only с публикацией порта на `127.0.0.1:8000`. Node/npm не требуются для расчёта и просмотра готового выпуска. Команды сборки/просмотра здесь — контракт HA-15, их фактическую готовность подтверждает README после проверки.

## 2. Вход и валидация

| Файл | Поля |
|---|---|
| `nodes.parquet` | `gid:int64`, `depth:int`, `is_seed:bool` |
| `edges.parquet` | `src:int64`, `dst:int64`, `sum_kzt:float64`, `n_tx:int64`, `depth:int8` |
| `transactions.parquet` | `src:int64`, `dst:int64`, `date:date`, `sum_kzt:float64` |

Проверки T-01:

- Обязательные поля присутствуют и имеют совместимые типы; отсутствующие значения в них запрещены. ID не проходят через float.
- `gid` уникален; пары `(src,dst)` в edges уникальны; все endpoints имеются в nodes, в том числе endpoints транзакций.
- `nodes.depth` в 0–4, `edges.depth` в 1–4, `is_seed` согласован с `depth=0`; даты разбираются и входят в июль 2026.
- Суммы конечные и положительные; транзакции ≥5 000 KZT; `n_tx` — положительное целое.
- Проверить точность до тиына, затем использовать целые `sum_tiyn=round(sum_kzt*100)`. Допуск представления исходного float документировать; сравнения агрегатов выполнять в целых тиынах.
- Группировка транзакций по паре должна совпадать с edges **по набору пар, сумме и количеству**. Сверка только наличия пар недостаточна.
- Совпадающие строки транзакций не удалять: у входа нет `transaction_id`, а одинаковая дата/сумма/пара не доказывает дубль.

Контрольные свойства официального архива: 2 248 узлов, 3 119 рёбер, 4 840 транзакций, 81 seed, 19 изолятов; оборот **36 589 001 201 тиын**. Полный граф имеет 35 слабых компонент, 444 узла depth=4 без исходящих. Это контроль приёмки на выданном наборе, не списки ответов и не ограничения внутренних тестовых графов.

`nodes.depth` — глубина узла, `edges.depth` — шаг обнаружения ребра. В текущем архиве `edges.depth = nodes.depth[src] + 1`; получатель может находиться на более раннем слое. Поступления на frontier всегда фильтруются через `nodes.depth[dst] == 4`.

## 3. Признаки

T-02: сначала добавить **все** `nodes.gid` в `DiGraph`, затем направленные агрегированные рёбра. Не строить граф перезаписью отдельных транзакций.

| Поле | Определение |
|---|---|
| `in_deg`, `out_deg` | Число различных соседей по направлению |
| `in_tiyn`, `out_tiyn` | Суммы наблюдаемых входящих/исходящих |
| `in_tx`, `out_tx` | Сумма `n_tx` в соответствующем направлении |
| `pass_through` | `out_tiyn/in_tiyn`; при нулевом входе — неизвестно, не 0 |
| `boundary` | `depth=4` и `out_deg=0` |
| `isolated` | Обе степени равны 0 |
| `seed_reach_count` (`S`) | Число различных seed, достигающих узла за 1–4 направленных шага; сам узел исключён |
| `betweenness` (`B`) | Точная, направленная, невзвешенная, `normalized=True`, `endpoints=False` |
| `last_in` | Максимальная календарная дата входящей транзакции, иначе неизвестно |
| `days_after_last_in` (`D`) | Разность 31.07.2026 и `last_in` в календарных днях, иначе неизвестно |

Для `S`: отдельный ограниченный BFS на каждый seed, без многократного счёта из-за циклов. Это достижимость в месячном графе без проверки дат; датированные counts и маршруты §10.5 её не заменяют. `sum_kzt` не использовать как расстояние при betweenness. Время внутри дня неизвестно.

Входящий профиль неполон для любого узла. Исходящие глубин 0–3 наблюдаются только в пределах банка, июля и порога выгрузки; это не полный финансовый баланс. Наблюдаемые признаки P1 определены в §10 и не подменяют эти ограничения.

## 4. Роли и поддержка

T-03: порядок выбора основной роли: **coordinator → distributor → consolidator → transit → terminal → peripheral**. Рассчитывать также все совпавшие роли; UI показывает их как вторичные. Квантиль — по полному графу, `linear`.

| Роль | Условие | `u` | `cap` |
|---|---|---|---:|
| coordinator | `S≥2`, `in_deg≥2`, `out_deg≥2`, `B>0`, `B≥Q90(B>0)` | `min(1,S/4)` | 0.60 |
| distributor | `out_deg≥10` | `min(1,out_deg/20)` | 0.90 |
| consolidator | `in_deg≥3` | `min(1,in_deg/6)` | 0.90 |
| transit | Не seed, depth<4, вход/выход положительны, `0.8≤r≤1.2` | `max(0,1-abs(r-1)/0.2)` | 0.65 |
| terminal | Не seed, depth<4, вход>0, out_deg=0, D≥2 | `min(1,D/7)` | 0.50 |
| peripheral | Нет совпадений | — | — |

Для каждого совпавшего правила `support=cap*(0.5+0.5*u)`. `role_score` — support **основной** роли; для peripheral 0. Если множество положительных B пусто, coordinator не срабатывает. Не использовать `NaN` как истинное условие.

Это зафиксированный baseline эвристик, а не калиброванные вероятности. Изменение порога требует общего правила, версии параметров и записи причины; не допускаются исключения по ID.

Границы: depth=4 не может стать terminal по нулевому выходу, но может быть consolidator по наблюдаемым входящим. Seed не становится transit/terminal на основе неполного входа. Изолят остаётся peripheral со своим кластером. `O>I` не создаёт дополнительной обвинительной роли.

Оценка скрытого выхода по бакетам/ML не отменяет запрета terminal на frontier. SCC и число сообществ плательщиков — контекст для проверки, а не дополнительные голоса в coordinator. Первичная роль выбирается по precedence; все вторичные совпадения сохраняются. Нулевая поддержка peripheral обозначает неустановленную роль, включая изолят.

## 5. Приоритет и объяснение

T-04: `P=0.35*M+0.30*A+0.20*C+0.15*H`.

- `M` — максимум support по всем совпавшим ролям, иначе 0.
- **`V_kzt=(in_tiyn+out_tiyn)/100`**; `A=min(1,log1p(V_kzt)/log1p(Q95(V_kzt>0)))`. Единица нормировки — KZT; нельзя незаметно заменить её тиынами внутри логарифма.
- `C=min(1,S/5)`.
- `H=min(1,B/Q95(B>0))`.
- При пустом положительном множестве/нулевом знаменателе соответствующая компонента 0. Каждый компонент конечен и в [0,1].

Сортировка: полный неокруглённый P по убыванию, точный числовой `gid` по возрастанию. Округление только для экрана. Сохранять компоненты и пороги для проверки.

P1 не добавляет множители только за seed/frontier и не использует SCC, близость суммы к 5 000 KZT или непроверенные вероятности для изменения P. 5 000 KZT — порог выгрузки, не установленный этим набором регуляторный порог. Автоматические юридические коды в evidence/why/hypothesis не присваиваются.

`evidence`: человеческий текст до 200 Unicode-символов с решающим правилом основной роли, фактическим значением/порогом и значимым ограничением. Формировать из тех же метрик; не обрезать строку посередине числа/смысла. Нулевой объём/отсутствие рёбер — тоже числовое объяснение. Шаблонный текст не должен приписывать клиенту вину.

### 5.1. Объяснения правил — HA-17.1, P0

`assign_roles` дополняет каждый элемент `matched_roles` полем `reason:string`: `{role,support,reason}`. Причина содержит реальные значения условий и фактически использованные пороги из parameters, включая вычисленный Q90 для coordinator. При peripheral список остаётся пустым; его объяснение находится в evidence. Нового классификатора или универсального языка правил не требуется.

| Основная роль | Основание evidence и подробного reason | Обязательная граница смысла |
|---|---|---|
| coordinator | S, входящая/исходящая степени, B и фактический Q90; в кратком evidence — решающие числа, полный набор в reason | Гипотеза структурной координации |
| distributor | Число получателей и порог ≥10 | Наблюдаемая выборка, цель переводов неизвестна |
| consolidator | Число плательщиков и порог ≥3 | Общность контроля не установлена |
| transit | Не seed, depth<4, положительные вход/выход и отношение в [0.8,1.2] | Неполный баланс, происхождение денег неизвестно |
| terminal | Не seed, depth<4, вход>0, выходящих соседей 0, D≥2 | Только наблюдаемый период и фрагмент |
| peripheral | Совпадений правил 0; наблюдаемые степени/суммы, граница либо отсутствие рёбер по применимости | Роль не установлена, безопасность не оценена |

`compute_priority` формирует `why` по двум крупнейшим взвешенным вкладам `.35M/.30A/.20C/.15H`; при равенстве порядок M,A,C,H. Для каждого назвать наблюдаемый факт: роль с максимальным support (может отличаться от основной), объём V в KZT, S либо B и соответствующий порог. При равном support выбрать роль по §4. Добавить значимое ограничение. Одних букв M/A/C/H и их нормированных чисел недостаточно. Параметры, роли, скоры и порядок top-20 при этой правке не меняются.

## 6. Сообщества

T-05: ненаправленный вес `{u,v}` равен сумме обоих направлений **в KZT**; это отдельная проекция, исходные направления сохраняются. Использовать Louvain `resolution=1`, `seed=42` и отсортированный ввод. Изолятам дать отдельные группы. Несвязное сообщество разделить на связные компоненты проекции.

`cluster_id`: группы сортируются по минимальному числовому gid, затем получают номера **0..K−1**. Номера воспроизводимы для фиксированного набора, не обязаны сохраняться при изменении входа.

Сводка: `n_nodes`, `n_seed`, внутренний оборот по исходным направленным рёбрам, до пяти лучших gid по P и осторожная гипотеза с числами. Внешний оборот не включается во внутренний. Сумма внутренних и межкластерного оборотов равна общему. Группа без seed не объявляется безопасной.

### 6.1. Первый узел для проверки — HA-18, P1

В существующий `hypothesis` неизолированной группы добавить: «Первый для проверки по P: <gid>, роль <role>; <in_deg> плательщиков, <out_deg> получателей; вход <KZT>, выход <KZT>». Узел берётся строго из `top_gids[0]` по прежнему порядку P↓/точный числовой gid↑; роль и показатели — из рассчитанных признаков или того же G. Суммы форматируются из целых тиынов существующим `_format_kzt`. Это все наблюдаемые потоки узла, включая внешние для кластера; их нельзя называть внутренним оборотом группы.

Сохранить размер/seed/внутренний оборот и оговорки; у изолята оставить описание отсутствия наблюдаемых связей, у группы без seed — отсутствие вывода о безопасности. Лидер по P не объявляется установленным организатором. Тот же текст используется в CSV и JSON/существующем UI. Новые поля, расчёт приоритета и отдельный экран не нужны.

Подготовка этой самостоятельной текстовой задачи допустима после HA-04.2/HA-05.1 при свободном `hackalem/clusters.py`, независимо от цепочки P1-пар. Включение — после P0/React/HA-17 с review до freeze 16:30; при нехватке времени отложить. Проверить членство и точность gid, роль/степени/суммы, ties, изолят и отсутствие seed. Числа, назначения кластеров и top_gids сохраняются; изменение текста закономерно меняет хеш clusters.csv.

## 7. CSV-контракты

UTF-8, заголовки, разделитель запятая, корректное quoting, точка в дробных числах. Полные ID — десятичная запись без экспоненты/`.0`. Обязательные поля не пусты; не добавлять индекс DataFrame как колонку.

| Файл | Обязательные поля и порядок |
|---|---|
| nodes_roles.csv | `gid,role,role_score,cluster_id,priority_score,evidence` |
| clusters.csv | `cluster_id,n_nodes,n_seed,sum_kzt_internal,top_gids,hypothesis` |
| top_nodes.csv | `rank,gid,role,priority_score,why` |

T-06: nodes отсортированы по числовому gid, clusters — по cluster_id. Scores — конечные числа [0,1]. `top_gids` — JSON-массив **строк**, корректно заключённый в CSV-ячейку. `sum_kzt_internal` выводится в KZT с двумя знаками. Дополнительные документированные поля допустимы после обязательных.

Для официального входа top_nodes содержит первые **20** узлов, rank=1..20. На малом внутреннем тестовом графе допустим top `min(20,N)`; это не ослабляет приёмку официального набора. Роль и P каждой строки совпадают с nodes_roles. Нет отдельного «демонстрационного» расчёта топа.

## 8. Контракт React / Python

T-07: Python формирует один объект для отчёта; из него пишутся три CSV и `report.json`. React один раз загружает соседний `./report.json` с того же localhost и отвечает за просмотр и фильтрацию, не присваивает роли заново. Python не генерирует JSX/DOM и не подставляет JSON в HTML. Данные не включаются в frontend build.

| Ключ | Содержимое |
|---|---|
| `schema_version` | Строка `1.0` |
| `dataset` | Период, размеры входа, оборот в тиынах, SHA-256 каждого входного файла |
| `parameters` | Версия правил, пороги, веса, квантили, seed и resolution |
| `nodes[]` | `gid:string`, role, role_score, cluster_id, priority_score, evidence; все признаки §3, matched_roles, score_components, warnings |
| `edges[]` | `src:string`, `dst:string`, `sum_tiyn:int`, `n_tx:int`, depth |
| `clusters[]` | Свойства сводки; `top_gids` как массив строк, не повторно закодированный JSON |
| `top_nodes[]` | rank, `gid:string`, role, priority_score, why |
| `daily_profiles_by_gid` · P1 | Необязательный словарь дневных наблюдений по §10.1; ключи — строковые gid |
| `community_edges[]`, `sccs[]`, `coverage` · P1 | Необязательные структурные сводки по §10.2 |
| `boundary_gids_by_inflow:string[]` · P1 | Необязательный индекс frontier для представления запросов по §10.3 |
| `seed_routes_by_gid` · P1 | Необязательные маршруты с датами по §10.5; ключи — строковые gid |

`score_components={M,A,C,H}` содержит нормированные значения; веса лежат в parameters. `matched_roles` содержит role, support и reason по §5.1. React отображает reason; у старого JSON без reason — evidence и отсутствие детализации, без повторной классификации. `warnings` содержит коды `boundary`, `seed_incomplete_in`, `isolated`, `short_followup` по применимости; общая неполнота графа сообщается на уровне отчёта. Даты — `YYYY-MM-DD` либо `null`.

Дополнительные поля `nodes[]` перечислены в §10. Они не обязательны для P0. UI проверяет наличие соответствующего блока; отсутствующий признак не превращается в 0 и не объявляется рассчитанным. Неизвестные дополнительные поля можно игнорировать; смысл и типы базовых полей `1.0` не меняются. Метаданные `parameters` записывают фактически реализованные группы P1 и их определения, без флагов готовности для ещё не созданных функций.

**Все клиентские ID преобразуются в строки до JSON.** Не использовать JS Number/parseInt для `gid/src/dst`; рассчитанные cluster_id/scc_id остаются небольшими целыми. Текущие суммы в тиынах укладываются в безопасный целочисленный диапазон JS; перед сериализацией это проверяется. Неопределённые величины — `null`, не `NaN/Infinity`. Сериализация JSON запрещает нечисла; тексты выводятся как текстовые JSX-значения, без `innerHTML`/`dangerouslySetInnerHTML` из данных.

T-08: React, Cytoscape.js и необходимые JS/CSS входят в локальный build; версии и лицензии сохранены. Нет CDN, шрифтов/телеметрии/аналитики с внешними запросами. Браузер открывает каталог выпуска через локальный статический сервер. `file://` и единый самодостаточный HTML больше не являются требованиями. CSP должна разрешать свои статические ресурсы и `connect-src 'self'` для JSON вместо прежнего `connect-src 'none'`, не разрешая внешние источники. Поиск/фильтры/состояния — по PRODUCT_SPEC §4.

React владеет выбранным строковым `selectedGid`, фильтрами и режимом просмотра; таблица, поиск, карточка и граф используют это общее состояние. Достаточно `App`, таблицы/карточки и `GraphView`; новые компоненты выделять по реальной необходимости. Только `GraphView` управляет imperative API Cytoscape.js через ref/effect: один экземпляр на mount, обновление данных/выделения без пересоздания при каждом клике, освобождение обработчиков и `destroy()` при unmount. Отдельный React-wrapper библиотеки не нужен. Цикл подключения/очистки соответствует [React useEffect](https://react.dev/reference/react/useEffect).

Дополнение UX 23.09.2026: на широком экране список, граф и карточка стоят рядом; на телефоне после поиска идут карточка, граф, список. Окрестность размещает выбранный узел между входящими и исходящими контрагентами; по умолчанию рисуются до 32 крупнейших по сумме рёбер, рядом видны исходное число рёбер и действие «Показать все». Таблица связей содержит полный результат фильтра без этого визуального ограничения. Полный граф размещается кругами по кластерам без физической симуляции и открывается обзором; выбор узла приближает его, «Вписать» возвращает обзор. Граф использует аппаратное разрешение canvas и векторные элементы управления.

Вход сборки — `frontend/index.html` и `frontend/report.html` с одним React entry; результат — одинаковые `frontend/dist/index.html` и `frontend/dist/report.html` и локальные ресурсы с относительными путями (`base: './'`). [Vite build](https://vite.dev/guide/build) создаёт статику; Node нужен для разработки/сборки. `vite dev` и [vite preview](https://vite.dev/guide/static-deploy) не являются запуском сдаваемого приложения. UI проверяет схему, обязательные массивы и строковые ID до создания графа; ошибка загрузки/JSON/версии видима, без подстановки учебных данных. Имеющиеся P1-поля переносятся без переименования и без обязательного наличия.

### 8.1. Читаемость графа и приоритета — HA-17.2, P0

- Для положительных сумм `a=sum_tiyn` ширина ребра: `1.25 + 3.25*(log1p(a)-log1p(a_min))/(log1p(a_max)-log1p(a_min))`. Min/max берутся один раз по **всем** report.edges. При равных min/max ширина 2.875; при пустом графе рёбер нет. Фильтр/выбор узла не меняет шкалу. Легенда сообщает логарифмическую шкалу; точная сумма доступна в подсказке и таблице. У будущего мета-графа — своя явно подписанная неизменная шкала по всем community_edges.
- Фильтр «основная роль / любое совпавшее правило» использует node.role / matched_roles соответственно; peripheral определяется основной ролью. Оба режима работают по полному индексу узлов. Совпадение вторичной роли не меняет основную роль, score или rank.
- Для выбранного P показать число узлов с **точно равным сохранённым score** и доступ ко всем таким узлам. Равенство округлённых подписей недостаточно. Фильтровать уже отсортированный Python массив nodes, сохраняя его порядок gid; JS Number для ID запрещён. Узлу вне top_nodes не присваивать глобальный rank. CSV остаётся top-20 по §7, включая прежнее разрешение равенства на границе.

## 9. Приёмка и воспроизводимость

T-09: один `test_contract.py` без нового фреймворка проверяет реальные выгрузки, множество ID, скоры, сортировку, evidence, сводки, сопоставление трёх CSV и несколько малых граничных графов. Включить невалидный endpoint/сумму/count, точный большой ID, изолят, seed без входа, границу, встречные рёбра и конфликт ролей.

HA-09.1 явно указывает запуск как CLI: `python test_contract.py --data ./data --out ./out`; pytest не является способом запуска этого скрипта. Поддерживаемый Python остаётся 3.13; дополнительную версию заявлять только после чистой установки закреплённых зависимостей и приёмки того же SHA. Проверка коллеги на Python 3.11 с другими версиями NumPy/NetworkX не расширяет поддерживаемый диапазон.

T-10: чистая установка/сборка по README, batch и localhost-просмотр без интернета, два одинаковых семантических выпуска. Node/build измерять отдельно от batch. До сдачи выполнить пять полных запусков из raw; показать median/maximum и характеристики машины. Порог <300 секунд относится ко всем обязательным CSV; внутренний тест включает JSON и копирование готового UI. Хеши CSV сравнивать при фиксированном форматировании; динамическое время не включать в семантическое сравнение.

После HA-15 test_contract.py читает report.json и сверяет CSV/assets вместо HTML-маркеров. На одинаковом аналитическом SHA миграция сохраняет CSV побайтно; после HA-17 тексты evidence/why меняются, а числовые роли/support/P/rank — нет. Реальный browser smoke через localhost проверяет canvas/стрелки, поиск, состояния/ошибки JSON, клавиатуру, CSV и отсутствие внешних запросов.

При включении HA-18 дополнительно сверить §6.1 в том же проверочном сценарии: разрешено изменение hypothesis/хеша clusters.csv; cluster_id, top_gids, все числовые сводки, роли/support/P/rank и остальные CSV при фиксированном аналитическом SHA сохраняются. Итоговая HA-08.2 фиксирует, включена или отложена HA-18; отсутствие необязательной задачи не блокирует выпуск.

`validation.json`: версия Python/пакетов, входные хеши, проверенные размеры/суммы, параметры, длительности этапов, итог выполнения проверок. Не записывать секреты и полный транзакционный журнал. Статус «успех» только после завершения выпуска.

Критерии качества ролей проверяются ручным review и сравнением с baseline. Ground truth нет: accuracy/F1 и вероятность преступления не заявляются. Тесты правил не являются измерением AML-точности.

HA-17 дополняет тот же test_contract.py: факты/пороги всех шести объяснений, secondary reason при конфликте, связь why с ведущими вкладами, evidence ≤200, неизменность roles/P/top-20. Ручная приёмка: три произвольных gid за минуту; разные суммы различаются толщиной; равные суммы и фильтрация сохраняют шкалу; режим вторичных правил и просмотр всех равных P доступны с клавиатуры, включая узел за пределами top-20. Старый report без reason читается без выдуманной детализации.

При реализации P1 тот же проверочный сценарий дополняется сверкой дневных сумм/counts с узлами, направленных межкластерных сумм с исходными рёбрами, членства/сводок SCC, условий запросов данных и маршрутов §10.5. Проверить UI как с доступными P1-блоками, так и на минимальном P0-объекте. Размеры SCC, состав top-20 и число ролей не задаются в логике константами. Полная приёмка HA-08.2 выполняется после HA-15.2/15.3, HA-17.2 и последнего включённого P1; уже пройденные проверки повторяются при изменениях, затрагивающих их результат.

## 10. Принятые дополнения P1

Дополнения реализуются после успешных HA-07.2 + HA-08.1 + HA-15.2 + HA-15.3 + HA-17.2, до заморозки новых функций; HA-08.2 принимает окончательный состав выпуска. Все суммы — целые тиыны, все клиентские ID в JSON — строки. P1 не добавляет обязательных CSV, аналитических библиотек или второго UI. Уже взятые расчёты можно довести по handoff; новый P1-просмотр расширяет React после указанного барьера. Порядок целых пар — **HA-11 → HA-14 → HA-16 → HA-13**; номера разделов сохранены для прежних ссылок. Оценка HA-12 может готовить baseline независимо; review новых объяснений — после HA-17.2.

### 10.1. Наблюдаемые признаки и дневные профили — HA-11

| Дополнительное поле узла | Определение |
|---|---|
| `n_seed_payers:int` | Число разных непосредственных предшественников в G с `is_seed=True`; не число seed-путей и не число сообществ |
| `sync_payers_max:int` | Максимум числа различных src по `(dst,date)` в transactions; 0 при отсутствии входящих |
| `fanout_burst_max:int` | Максимум числа различных dst по `(src,date)`; 0 при отсутствии наблюдаемых исходящих |

`daily_profiles_by_gid[gid]` — список `{date,in_tiyn,out_tiyn,in_tx,out_tx,n_payers,n_payees}` по дням хотя бы с одной наблюдаемой операцией, в возрастающем порядке. Ключ есть у каждого узла; у изолята список пуст. В строке отсутствующее направление даёт 0; для отображения календарной оси UI может заполнить пропущенные дни нулями наблюдений только у выбранного узла. Число контрагентов считается по уникальным ID, counts — по всем строкам transactions, включая совпадающие. Сумма дневных in/out и counts совпадает с узловыми агрегатами; дневные числа уникальных плательщиков нельзя складывать как месячный in_deg.

Нули обозначают отсутствие операций в выгрузке. У frontier исходящий профиль сопровождается указанием, что исходящие не наблюдались. Синхронность по дате не устанавливает порядок операций или время удержания. Дневной профиль показывается в существующей карточке через HTML/SVG/таблицу без новой библиотеки.

### 10.2. Сообщества, SCC и покрытие — HA-13

- `nodes[].n_payer_comms:int` — число разных `cluster_id` непосредственных плательщиков, включая собственное сообщество, если из него есть вход; 0 у узла без входа.
- `community_edges[]` — `{src_cluster_id,dst_cluster_id,sum_tiyn,n_edges,n_tx}`. Одна строка на упорядоченную пару **разных** сообществ; встречные направления раздельны. `n_edges` — число исходных агрегированных рёбер, `n_tx` — сумма их counts. Сортировка по двум ID. Внутренний оборот берётся из `clusters[]`; его сумма плюс сумма `community_edges.sum_tiyn` равна обороту графа.
- SCC вычисляются в исходном направленном G, включая singleton-компоненты. `scc_id` задаётся по минимальному числовому gid, аналогично cluster_id, но в отдельном пространстве. Каждый узел получает `scc_id:int,scc_size:int`; связные сообщества Louvain не заменяют SCC.
- `sccs[]` — `{scc_id,n_nodes,n_seed,n_edges_internal,n_edges_incoming,n_edges_outgoing,sum_tiyn_internal,sum_tiyn_incoming,sum_tiyn_outgoing,n_external_recipients,origin_turnover_share}`. Вход/выход учитывает только пересечение границы данной SCC; внутренние рёбра считаются отдельно. Получатели — уникальные dst вне SCC. `origin_turnover_share=(internal+outgoing)/общий_оборот`, при нулевом обороте 0. Строки по scc_id; состав определяется через поля узлов, повторный список gid не нужен.
- `coverage` — `{boundary_n_nodes,sum_tiyn_to_boundary,share_to_boundary}` по узлам depth=4 и исходным рёбрам к ним. При нулевом общем обороте доля 0. Нулевая наблюдаемая исходящая активность не трактуется как удержание денег.

UI рисует направленный мета-граф по `clusters[]` и `community_edges[]`; выбор открывает состав и исходные связи. Отдельный режим подсветки SCC использует тот же узловой граф. По умолчанию SCC-обзор выбирает наибольшую компоненту с `n_nodes>1`, при равенстве минимальный scc_id; если таких нет, это явно сообщается. Клик по агрегированной связи раскрывает соответствующие исходные рёбра. Все сводные суммы вычисляет Python; UI фильтрует и форматирует результат.

Сверки текущего официального входа из [аудита](../research/blueprint_comparison_evidence.json): крупнейшая SCC — 85 узлов, 2 seed, 242 внутренних ребра на 19 453 337,04 KZT; 875 внешних исходящих рёбер на 90 230 006,09 KZT к **642** разным получателям; 7 внешних входящих рёбер на 705 193,00 KZT. Доля оборота, исходящего от членов SCC, — 29,98%, включая внутренние переводы. Поступления на frontier — **56 672 164,68 KZT / 15,49%**. Это эталоны сверки агрегатов для этих файлов, не условия классификации и не доказательство преступной структуры.

### 10.3. Следующий запрос данных — HA-14

`nodes[].next_data_requests` — список `{reason_code:string,text:string}`, рассчитанный Python по таблице ниже в указанном порядке. Применяются все подходящие условия; пустой список допустим. В тексте указать наблюдаемое основание и запрос, без выдуманных сведений о клиенте. Это подсказка для человека, не сетевой вызов.

| reason_code | Условие | Запрос |
|---|---|---|
| `boundary` | depth=4 | Следующий исходящий слой для выбранного узла с указанием банка, периода и порога; текущий выход неизвестен |
| `short_followup` | in_tiyn>0, out_deg=0, days_after_last_in<2 | Продлить окно минимум до двух дней после последнего поступления; указать дату поступления |
| `incomplete_balance` | Есть наблюдаемые входящие или исходящие | Полный вход и начальный/конечный остатки для проверки баланса; текущие суммы ограничены выборкой |
| `same_day_order` | Есть день с in_tx>0 и out_tx>0 в дневном профиле | Временные метки переводов для проверки порядка внутри дня; указать первую такую дату |
| `isolated` | in_deg=out_deg=0 | Уточнить покрытие выгрузки по этому gid, включая операции вне банка/периода/порога; отсутствие записей не объясняет причину |

Представление «Запросить продолжение» — фильтр **всех** узлов depth=4, сортировка in_tiyn ↓, затем точный числовой gid ↑, рассчитанная Python или порядком строк из Python для UI. Строковые ID не сравнивать через JS Number. Для этого P1-блока Python передаёт `boundary_gids_by_inflow:string[]` в указанном порядке; это индекс существующих узлов, не ещё один скор или CSV. Исходный priority_score и глобальные ранги top_nodes сохраняются. У узла вне top_nodes глобальный rank отсутствует и не придумывается.

### 10.4. Оценка и последующие изменения — HA-12

Baseline — формула §5 и отдельный топ по `in_tiyn+out_tiyn`. Review получает объединение двух top-20 и контрольную выборку с фиксированным seed, без названия метода и ранга. Фиксируются проверяемость оснований, корректность направления/неполноты, полезность следующего запроса, время задачи, размер выборки и компетенция рецензента. Overlap и доля non-seed описывают выборку, но сами по себе не измеряют полезность. Устойчивость — изменения порогов/весов ±20% с перенормировкой весов; для сообществ сравнивать членство, не только число групп.

Добавление контекста в P1 не меняет top-20. Предлагаемый новый скор сравнивается отдельно: исходный baseline → вариант с временными признаками → со структурными → объединённый. Определения/веса и критерии review фиксируются до просмотра результата; выигрыш обосновывается review, а не отличием списков или повышением встроенных в скор метрик.

Следующий временной эксперимент переиспользует проверенный подход из `research/compare_blueprints.py`: FIFO с расходованием каждого тиына входа и выхода не более одного раза, окно **1–2 последующих календарных дня**. Сопоставленный объём делится на наблюдаемый исходящий; при отсутствии выхода результат неизвестен (`null`). Same-day допускается только отдельным явно названным предположением. При доле пересланного входа исключать из знаменателя поступления без полного последующего окна и показывать их отдельно. Сигнал «есть любое поступление за 0–2 дня до выхода» можно считать совпадением по времени, но нельзя называть FIFO. Перед включением в продукт проверить суммы, повторное использование, same-day и конец периода; продемонстрированные исследовательские количества не являются эталоном AML-ролей.

Обратимо менять параметры допустимо до заморозки по результатам проверки, с обновлением версии, спецификации и связанных задач. ID-исключения, замена входа внешним датасетом и неотражённое изменение формулы запрещены. Все ещё не реализованные функции остаются задачами, а не отмечаются готовыми по наличию этой спецификации.

### 10.5. Маршруты от seed с датами — HA-16

`nodes[]` получает `seed_reach_strict_count:int` и `seed_reach_same_day_count:int`: число различных seed, достигающих узла за **1–4 направленных перевода** по исходным транзакциям. Сам исходный seed исключён из собственного результата, в том числе при цикле. `strict` требует строго возрастающих дат, `same_day` — неубывающих; порядок внутри одного дня неизвестен. Ожидание между переводами не ограничено 1–2 днями: допускается весь наблюдаемый июль. Эти поля не заменяют месячный S, не меняют роли/P и не сопоставляют суммы как одни и те же деньги.

Необязательный корневой блок `seed_routes_by_gid` содержит каждый gid при реализованной функции. Значение:

```text
{
  strict: null | {seed_gid: string, steps: [step, ...]},
  same_day: null | {seed_gid: string, steps: [step, ...]}
}
step = {src: string, dst: string, date: "YYYY-MM-DD", sum_tiyn: int, tx_row: int}
```

`tx_row` — нумерация с нуля в **исходном порядке строк transactions.parquet**, привязанная к SHA-256 этого файла в dataset. Это адрес строки для проверки свидетеля, не выдуманный банковский transaction_id. Присвоить до сортировки; одинаковые строки не удалять. Сумма положительная, в точных тиынах и допустимом JSON integer диапазоне.

На узел хранить не более **одного** свидетеля для каждого режима, с 1–4 непрерывными шагами. Для выбора: минимальный точный числовой seed с маршрутом; для него минимальное число шагов, затем наиболее ранняя дата окончания. Обход строк фиксирован: `(date, numeric src, numeric dst, sum_tiyn, tx_row)`; при равных состояниях сохраняется первое. Не перечислять все пути. Достаточны раннее достижение и один предшественник для `(seed, число шагов, узел)`, отдельно для каждого режима; переиспользовать подход `dated_reach` из исследовательского скрипта. Counts учитывают все подходящие seed независимо от ограничения числа свидетелей.

Проверки в существующем test_contract.py при реализации:

- `0 ≤ strict_count ≤ same_day_count ≤ seed_reach_count`; count=0 соответствует null, count>0 — валидному свидетелю. Каждый шаг совпадает со строкой входа, датой и суммой; первый src=seed_gid с is_seed, последний dst=gid.
- Обратный порядок дат исключён; одинаковые даты допустимы только в same_day; циклы не увеличивают число seed; самодостижимость seed не считается. Проверить разрыв графа, 4 против 5 шагов, несколько seed, одинаковые строки, большие ID и воспроизводимый выбор свидетеля.
- Изолят имеет оба count=0 и обоих свидетелей null. Отсутствие **всего** P1-блока означает «не рассчитано», не нулевую достижимость. Неполный или противоречивый рассчитанный блок — ошибка контракта.
- Контрольные суммы counts для исследованного входа: S=4 219, same_day=1 898, strict=1 623 ([источник](../research/second_wave_evidence.json)). Это сверка данного входа/определения, не константы алгоритма и не число ложных срабатываний AML.

`parameters.temporal_routes` фиксирует max_hops=4, сравнения дат `>` / `>=`, ожидание в пределах периода, witness_limit=1 и отсутствие атрибуции денег. Схема CSV не расширяется. Исследовательские итоги не означают готовности этой функции в pipeline.

UI в той же карточке различает месячный S, число прямых seed-плательщиков и оба датированных count. Свидетель показывается списком точных ID, дат и сумм с переходом к узлам/подсветкой в существующем графе; таблица шагов остаётся доступна с клавиатуры. Подписи: «при неизвестном порядке внутри дня» для same_day; «маршрут до 4 переводов по заданному правилу дат не найден» для null; «не рассчитано» при отсутствии блока. Суммы шагов не суммируются в «прослеженный объём», отсутствие пути не означает безопасность. Временная совместимость маршрута и SCC месячного графа показываются как разные факты.

## 11. Точки реализации для агентов

В `main` аналитика уже перенесена из плоского starter в `hackalem/` (исследованный рефакторинг `d58551f`). Использовать существующие модули; `starter.py` остаётся совместимым фасадом. Не создавать второй пакет, рендерер, сервисы или API. Ниже существующие стыки и принятые расширения; готовность определяется handoff и приёмкой.

| Модуль / функция | Вход → результат | Задача |
|---|---|---|
| `hackalem/validation.py`: `load(data_dir)` | Path → `(edges,nodes,tx)`, порядок starter сохранён | HA-01.1 |
| `hackalem/validation.py`: `sanity_check(edges,nodes,tx)` | §2, целые sum_tiyn в edges/tx; множество изолятов; ошибки ValueError с причиной | HA-01.2 |
| `hackalem/graph.py`: `build_graph(edges,nodes)`, `basic_features(G,nodes)` | Все узлы/направленные рёбра → df базовых признаков | HA-02.1 |
| `hackalem/graph.py`: `enrich_features(G,df,tx)` | df с S/B/last_in/D, исходные колонки сохранены | HA-02.2 |
| `hackalem/scoring.py`: `assign_roles(df)` | `(df,role_parameters)`; role/role_score/matched_roles; reason в каждом совпадении по §5.1 | HA-03.1 → HA-17.1 |
| `hackalem/scoring.py`: `compute_priority(df)` | `(df,priority_parameters)`; P/components/evidence/why; объяснения по §5.1 | HA-03.2 → HA-17.1 |
| `hackalem/clusters.py`: `cluster_nodes(G)` | Точный gid:int → cluster_id:int для каждого узла | HA-04.1 |
| `hackalem/clusters.py`: `summarize_clusters(G,df)` | df с cluster_id/P → сводки; sum_tiyn_internal:int, top_gids:list[int] | HA-04.2 |
| `hackalem/report.py`: `build_report(df,edges,clusters,metadata,parameters)` | Один JSON-совместимый dict §8; ID→строки, unknown→null | HA-05.1 |
| `hackalem/exports.py`: `write_outputs(report,out_dir)` | Три CSV по §7 из общего объекта | HA-05.2 |
| `hackalem/html.py`: `render_report(report,out_path)` | Локальные шаблон/ресурсы → автономный HTML, пути относительно кода | HA-07.1 |
| `hackalem/pipeline.py`: `run_pipeline(data_dir,out_dir)`, `main(argv=None)` | CLI --data/--out; все этапы, проверка, временный выпуск и validation.json | HA-07.1 |
| `hackalem/graph.py`: `enrich_observed_features(G,df,tx)` · P1 | `(df,daily_profiles_by_gid)`; §10.1 | HA-11.1 |
| `hackalem/requests.py` (принятый модуль HA-14.1): `build_data_requests(df,daily_profiles_by_gid)` · P1 | df с next_data_requests; индекс boundary_gids_by_inflow собрать из отсортированного df при выпуске | HA-14.1 |
| `hackalem/graph.py`: `enrich_temporal_routes(G,df,tx)` · P1 | `(df,seed_routes_by_gid)`; §10.5, исходный порядок tx сохранён | HA-16.1 |
| `hackalem/clusters.py`: `summarize_structure(G,df)` · P1 | `(df,structure)` с n_payer_comms/scc_id/scc_size и `{community_edges,sccs,coverage}`; §10.2 | HA-13.1 |

До правки найти всех вызывающих, включая фасад, pipeline и проверки. Смена публичной сигнатуры требует одновременного обновления здесь, в вызывающем коде и карточках. Один владелец каждого изменяемого файла; расширения общего report/pipeline согласовать с их текущим владельцем по PLAN §9.

`parameters` содержит фактические role_parameters/priority_parameters, веса, версию правил и Louvain. UI/экспорт не пересчитывают пороги. Для HA-17 обновляется версия объяснений в parameters, без новой формулы. SHA-256 входов и dataset metadata передаёт pipeline. В `clusters[]` sum_tiyn_internal — целое, sum_kzt_internal — десятичная строка с двумя знаками, top_gids — строки; CSV использует sum_kzt_internal.

### Стык интерфейса и интеграции

- HA-06/07 фиксируют историю текущего standalone HTML. Новая реализация идёт в HA-15.1 → HA-15.2 (`frontend/`); HA-15.3 отвечает за JSON, сборку/копирование и локальный просмотр. Старый renderer сохраняется до приёмки миграции, затем убирается из активного пути; два поддерживаемых UI не создаются.
- `report.example.json` остаётся явно учебным входом с точными строковыми ID >2^53, изолятом и boundary. Один и тот же UI может читать его в локальном preview как report.json; в релиз попадает только реальный build_report. Не копировать официальный датасет в frontend/public или Git.
- Интегратор HA-15.3 владеет `hackalem/pipeline.py`, изменением `hackalem/html.py`, `Dockerfile`, `compose.yaml`, `.dockerignore`; B владеет `frontend/`. Правки build_report для аналитических P1 передаются от их владельца отдельно. Пути ресурсов вычисляются относительно пакета/build, не cwd; существующие --data/--out сохраняются.
- HA-08.1 создаёт один `test_contract.py`: `python test_contract.py --data ./data --out ./out`. Проверка читает реальные файлы и включает малые граничные примеры через функции выше. Ручные browser-проверки и результаты пяти запусков записываются отдельно от автоматических утверждений.
- HA-11.1 расширяет main/build_report: переносит рассчитанные поля §10.1 в nodes и прикрепляет `daily_profiles_by_gid`; HA-11.2 отображает их в существующей карточке. Схема дневных строк включает `n_payers,n_payees`, определения и нули — по §10.1.
- HA-13.1 после кластеризации вызывает summarize_structure, передаёт расширенный df в build_report и прикрепляет community_edges/sccs/coverage к тому же report. HA-13.2 использует эти сводки для обзора, переходов к рёбрам и подсветки SCC.
- HA-14.1 вычисляет next_data_requests до build_report и прикрепляет boundary_gids_by_inflow; HA-14.2 выводит запросы и фильтр полной таблицы. Все включённые пары расширяют пример и проверки при реализации, а не создают второй формат или отдельный рендерер. Отсутствие P1-блока сохраняет P0-сценарий; рассчитанный блок проверяется до успешного выпуска.
- HA-17.1 расширяет matched_roles.reason/evidence/why; HA-17.2 после HA-17.1 + HA-15.2 + HA-15.3 реализует §8.1 в frontend/src/ (карточка, таблица, GraphView, CSS). Учебный JSON включает конфликт ролей и точные ties; нового viewer нет.
- HA-16.1 подключает enrich_temporal_routes до build_report, переносит оба counts и seed_routes_by_gid в тот же report.json; HA-16.2 после handoff расширяет React-карточку/граф. Отсутствующий блок читается как «не рассчитано».
- Порядок целых P1-пар: HA-11 → HA-14 → HA-16 → HA-13. Общие report/pipeline принадлежат текущему владельцу; C дополняет один test_contract.py и принимает фактический SHA по §9. Функции/скоры не переносятся в JSX.
