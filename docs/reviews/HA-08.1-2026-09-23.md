# HA-08.1 independent review

- Card: https://trello.com/c/VehOaQmX
- Reviewed test: `0547fbf3d2267949984913094b064add77101ef6`
- Reviewer: Codex root
- Verdict: the P0 baseline passes, but two malformed outputs are accepted by `test_contract.py`.

Baseline command (host Python 3.11.4):

```powershell
python test_contract.py --data G:\HACKATON\hack-e3bf6094-jigas\data --out G:\HACKATON\hack-e3bf6094-jigas\out\final1
```

Result: `PASS: synthetic boundary graph, exact-ID export, and current release outputs`. The prior Docker/Python 3.13 PASS remains documented in `docs/validation.md`.

On separate copies of `out/final1`, these mutations also returned PASS:

1. Set the first `clusters.csv` `cluster_id` from `0` to `0.5`. `validate_csv_release` applies `int()` after `pd.to_numeric`, silently truncating the fraction. The contract requires integer, contiguous IDs `0..K-1`.
2. Replace `validation.json` with UTF-8 JSON `{"status":"error"}`. The test checks only that the root is a dictionary; it does not assert a successful status or minimal manifest fields.

The original release files were not changed. Keep the card in review until the verification owner rejects both mutations while retaining the baseline PASS. At HA-15.3, update the test for `report.json` and the local UI build under the React contract.
