# HA-15.3 independent supplemental review

Follow-up, 23 September 2026: the favicon fix entered `main` as [`8e27beb`](https://github.com/BAITC-Hacks/hack-e3bf6094-jigas/commit/8e27bebc4a5f5b553e12f85a002d75f8b8cc5373), and packaging was corrected in `377ba44`. The resulting `release-final2` passed Docker pipeline, contract and localhost Playwright checks with the icon available over HTTP, as recorded in [validation.md](../validation.md). README smoke wording was corrected by PR #10. [HA-08.2](https://trello.com/c/9OP9LTPB) subsequently passed final clean-install acceptance; see the current snapshot in [PARALLEL_WORK.md](../PARALLEL_WORK.md). The findings below describe the earlier `1d27281` snapshot, including its favicon 404, and remain useful as dated evidence.

- Card: https://trello.com/c/TOLPtvoW
- Reviewed integrated snapshot: `1d27281d7681cd1e597672bc18436abb27036378` in an isolated worktree.
- Reviewer: Codex / GPT-6 Luna max, read-only.
- Verdict: native build, pipeline, contract and local asset paths passed. A local `/favicon.ico` 404 remains in this snapshot; [PR #9](https://github.com/BAITC-Hacks/hack-e3bf6094-jigas/pull/9) proposes the fix and needs a browser recheck on the resulting SHA.

Evidence:

- `npm ci` and `npm run build` — PASS. Vite warns about a roughly 913 kB JS chunk; the build succeeds.
- Official archive through `starter.py` and `test_contract.py` in a temporary Python 3.11.4 environment with pinned packages — PASS, including the synthetic negative JSON string-score probe. Result: 2,248 nodes, 3,119 edges, 89 clusters, and a success manifest covering the three CSVs, JSON, HTML and local assets.
- Local HTTP served every manifest path and CSV with status 200. `/favicon.ico` returned 404; the candidate has no icon declaration. This reproduces the HA-15.2 console finding.
- Docker, Python 3.13, a physically disconnected browser, and direct canvas-click selection were outside this supplemental review. The Python 3.11 `nodes_roles.csv` hash differed from the Docker reference; the runtime mismatch makes that comparison inconclusive. The documented five Docker runs are separate evidence in `docs/validation.md`.
- The reviewed README still called localhost smoke pending below a newer PASS claim. [PR #8](https://github.com/BAITC-Hacks/hack-e3bf6094-jigas/pull/8) aligns the wording with the actual validation evidence.

Next: integrate PR #9, repeat the browser console/network smoke on the accepted SHA, and retain the official Docker measurements as the supported-runtime evidence.
