# HA-17.1 independent review

- Card: https://trello.com/c/tIzQHdJ0
- Reviewed implementation: `7a6da9ba1fce66e8ed62a0756459564bac416e41` on `feat/ha15-explain`; base `origin/main` at `59ef08e622bd491b2b8d204096620b67bd0e19ce`.
- Reviewer: Codex / GPT-6 Luna max, independent read-only review.
- Verdict: PASS for HA-17.1; HA-17.2 may consume this JSON shape. This does not accept the React UI or the final release.

Checks on `G:\HACKATON\wt-ha15`:

- `git diff --check` — PASS.
- `python test_contract.py --data ./data --out ./out/ha17` — PASS on host Python.
- Official payload: 2,248 nodes and 1,359 matched-role entries; zero missing/blank `reason`; maximum `evidence` length 187 Unicode characters; zero over 200; all 20 `top_nodes[].why` entries contain observed fact labels.
- Recomputed roles, support and priority with `origin/main` scoring code over the official payload features: zero numeric mismatches with CSV at tolerance `1e-14`; top-20 IDs and order match. This is a recomputed baseline, not a preserved old-CSV byte comparison.
- Source review against SPEC 1.4 §5.1: matched-role reasons include observed values and thresholds; evidence states decisive rule and limitations; why names the leading weighted contributions and max-support role. The score formula and sort remain unchanged.

Remaining handoff: HA-17.2 must show the reasons in the React UI and verify readable edge sums/ties on a real release. Verification should add exact explanation semantics to `test_contract.py`; the current CLI PASS alone does not assert them.
